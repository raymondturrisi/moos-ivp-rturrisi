#!/bin/bash

if [ "$#" -ne 0 ]; then
	./kill_descendents.sh $1
fi

ktm &> /dev/null &
zkill.sh 
zkill.sh ~/moos/moos-ivp-swarm/bin
zkill.sh ~/moos/moos-ivp-pavlab/bin
zkill.sh ~/moos/moos-ivp-rturrisi/bin
pkill pAntler
pkill MOOSDB
pkill uMAC
